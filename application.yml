 eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8787/eureka/
  instance:
    hostname: localhost
    
  microservice:
   cart-service:
    endpoints:
      endpoint:
          uri: http://localhost:7676/cart
      
  search-service:
    endpoints:
      endpoint:
  uri: http://localhost:7777/search 